# ============================================================================
# Production Requirements - FastAPI Runtime Only
# ============================================================================
# Minimal dependencies for running SKU recognition API
# Excludes: training, data augmentation, MySQL, development tools

# Core ML/AI
torch>=2.0.0
torchvision>=0.15.0
open-clip-torch>=2.20.0
faiss-cpu>=1.7.4

# Image Processing (minimal)
Pillow>=10.0.0
numpy>=1.24.0

# FastAPI and Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
gunicorn>=21.2.0
python-multipart>=0.0.6

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0
